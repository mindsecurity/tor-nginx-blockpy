## Python Blockchain with Nginx over Tor

* Just execute the `docker-compose.yml` file
```
git clone https://github.com/mindsecurity/tor-nginx-blockpy.git
cd tor-nginx-blockpy
docker-compose up -d
```
